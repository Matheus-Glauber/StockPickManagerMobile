<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="StockPickManagerMobile.Views.NewItemPage"
             Title="New Action">
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancel" Clicked="Cancel_Clicked" />
        <ToolbarItem Text="Save" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="Código: " FontSize="Medium" />
            <Entry Text="{Binding Item.Name}" d:Text="Código da Ação" FontSize="Small" />
            <Label Text="Quantidade:" FontSize="Medium" />
            <Editor Text="{Binding Item.Qtd}" d:Text="Quantidade da Ação" FontSize="Small" />
            <Label Text="Preço:" FontSize="Medium" />
            <Editor Text="{Binding Item.price}" d:Text="Preço da Ação" FontSize="Small" Margin="0" />
        </StackLayout>
    </ContentPage.Content>
    
</ContentPage>
